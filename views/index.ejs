<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cohacsa Voting Center</title>
    <!-- Include Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    />
    <!-- Include Animate.css -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />
    <!-- Include AOS CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/aos@2.3.1/dist/aos.css"
      rel="stylesheet"
    />
    <style>
      body {
        background-color: #000;
        color: #fff;
        font-family: "Arial", sans-serif;
        text-align: center;
        margin: 0;
        padding: 0;
      }
      .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100vh;
        z-index: 1;
      }
      .logo {
        max-width: 150px;
        margin-bottom: 20px;
        animation: fadeInDown 1s ease-out;
      }
      h1 {
        font-size: 2.5rem;
        margin-bottom: 30px;
        animation: fadeInUp 1s ease-out;
      }
      .btn {
        font-size: 1.2rem;
        padding: 15px 30px;
        border-radius: 50px;
        transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
        animation: bounceInUp 1s ease-out;
      }
      .btn-get-in {
        background: linear-gradient(135deg, #e83e8c, #6b2181);
        border: none;
        color: #fff;
        box-shadow: 0 4px 15px #6b2181;
        margin-right: 10px;
      }
      .btn-get-in:hover {
        background: linear-gradient(135deg, #d63384, #6b2181);
        transform: scale(1.05);
        box-shadow: 0 6px 20px #6b2181;
      }
      .btn-about {
        background: linear-gradient(135deg, #2196f3, #21cbf3);
        border: none;
        color: #fff;
        box-shadow: 0 4px 15px rgba(33, 203, 243, 0.6);
      }
      .btn-about:hover {
        background: linear-gradient(135deg, #1976d2, #21cbf3);
        transform: scale(1.05);
        box-shadow: 0 6px 20px rgba(33, 203, 243, 0.8);
      }
      .about-section {
        display: none;
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        padding: 20px;
        background-color: #222;
        color: #fff;
        border-radius: 10px;
        max-width: 600px;
        z-index: 2;
      }
      .about-section h2 {
        margin-bottom: 20px;
        font-size: 1.8rem;
        color: #e83e8c;
      }
      .btn-back {
        background: linear-gradient(135deg, #ff9800, #ffeb3b);
        border: none;
        color: #000;
        font-size: 1rem;
        padding: 10px 20px;
        border-radius: 50px;
        transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
        margin-top: 20px;
      }
      .btn-back:hover {
        background: linear-gradient(135deg, #f57c00, #ffeb3b);
        transform: scale(1.05);
        box-shadow: 0 6px 20px rgba(255, 235, 59, 0.8);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <img
        src="/images/logo.png"
        alt="Cohacsa Logo"
        class="logo animate__animated animate__fadeInDown"
        data-aos="fade-down"
      />
      <h1 class="animate__animated animate__fadeInUp" data-aos="fade-up">
        Cohacsa Dinner Voting
      </h1>
      <div>
        <a
          href="/end"
          class="btn btn-get-in animate__animated animate__bounceInUp"
          data-aos="zoom-in"
          >Get In</a
        >
        <button
          class="btn btn-about animate__animated animate__bounceInUp"
          data-aos="zoom-in"
          onclick="showAbout()"
        >
          About
        </button>
      </div>
    </div>
    <div class="about-section" id="aboutSection">
      <h2>About Cohacsa</h2>
      <p>
        Cohacsa dinner is the upcoming and the latest party coming to the very
        campus in Ikire, it is a whole college dinner and awards night, the
        first of the kind you must not miss. You are here because you want to
        vote for your fav to get an award at this big occasion, so dive in now
        and vote before your chance runs out!
      </p>
      <button class="btn btn-back" onclick="hideAbout()">Back</button>
    </div>
    <!-- Include jQuery -->
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <!-- Include Popper.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <!-- Include Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <!-- Include AOS JS -->
    <script src="https://cdn.jsdelivr.net/npm/aos@2.3.1/dist/aos.js"></script>
    <script>
      AOS.init({
        duration: 1000, // Duration of the animation
        easing: "ease-in-out", // Easing function
        once: true, // Animate only once
      });

      function showAbout() {
        document.querySelector(".container").style.display = "none";
        document.getElementById("aboutSection").style.display = "block";
        document
          .getElementById("aboutSection")
          .scrollIntoView({ behavior: "smooth" });
      }

      function hideAbout() {
        document.querySelector(".container").style.display = "flex";
        document
          .querySelector(".container")
          .scrollIntoView({ behavior: "smooth" });
        document.getElementById("aboutSection").style.display = "none";
      }
    </script>
  </body>
</html>
